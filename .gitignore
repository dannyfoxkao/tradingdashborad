
# 回測快取與輸出（本地累積，不進版控）
__pycache__/
*.pyc
backtest_cache/*
!backtest_cache/shares_issued.csv
backtest_trades.csv
backtest_ignitions.csv
backtest_summary.json
backtest_trades_*.csv
backtest_ignitions_*.csv
backtest_summary_*.json

# ※ disposition_calendar.csv / turnover_leaderboard.csv 進版控：
#   Streamlit Cloud 檔案系統是暫時性的，歷史資料要靠 repo 帶上去；
#   本地累積新資料後記得 commit 更新。

# 原始處置/處分下載檔（整合進 disposition_calendar.csv 後即為衍生資料）
disposal_information_*.csv
punish.csv

# FinMind API 憑證（本地填入，勿上版控；Cloud 用 App Secrets: FINMIND_TOKEN）
finmind_token.json
